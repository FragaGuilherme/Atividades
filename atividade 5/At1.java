import java.lang.Math;
public class At1 {
    public static void main(String[] args) {
        double X1=3, X2=6, Y1=2, Y2=4, resul;

//=================================================================

        resul = Math.pow((X1 - X2),2) + Math.pow((Y1 - Y2),2);

//=================================================================

        System.out.print("O resultado é: " + Math.sqrt(resul));
    }
}
